pengine_rpc('http://localhost:5000',query,[options]) // this will call a query to the pengine at that server address

Can write to a file using open('file.name',write or append, Stream), write(Stream,'stuff to write'), nl(Stream), close(Stream).
//This can be used to create and write .pl files which are callable soo pretty cool.

Got it setup so there is a bill file for customers and a customers file. If I need to add customers just append to the
customers file, same with bill just append to the bill file.
        customerBill.pl // bill file 
        customers.pl // customer file

I created a function that can add to an existing file and essentially create a customer. Need to
determine how to edit an existing customer.

In Customer Rules there is a funciton save_each_customer. This will save all the facts about customer(Id,Name,Email,Phone,Status)
that is in memory into a written file(hopefully customers) The function needs some edits so that it keeps the customer module stuff
but this will work with the function that edits existing facts.

Need to write emails as strings as well, not sure how to do that quite yet.

Updated function, also if I need to change a term to a string it is with term_string(In,Out).
I ended up not needing to because I realized that I should always be putting a string into the create
customer so it should be fine as long as I operate that way.

When using Tau-Prolog I can consult files that are using modules but I can't directly access those usings?
Idk I was getting errors if I tried to use them from a file. Can't use dynamic in .pl files when loading into tau.

Thought of trying to use tau-prolog to edit the DOM but was unsuccesful, not much online that I could find
to help understand that portion of it better.

Having a hard time getting rules to work with tau-prolog from loaded files. Not sure what I am doing wrong.

Rules do work with Tau-Prolog there are just some predicates that I wanted to use that do not exist in tau-prolog
so I have to write my own.

bagof/3 will make lists that are seperate while findall/3 will aggregate the lists together

Got Pengine server working!!! I need to start the pengine server first then in the index.html it can be loaded using
var pengine = new Pengine({server: "https:location/of/server:5050"})
here is a good link to help with running it https://swi-prolog.discourse.group/t/how-to-run-pengines-and-call-it-from-external-server/4522/3
Another link I should look at https://www.swi-prolog.org/pengines/GenealogistGUI.md